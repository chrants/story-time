<h1>Listing admin_uploads</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_uploads.each do |admin_upload| %>
  <tr>
    <td><%= link_to 'Show', admin_upload %></td>
    <td><%= link_to 'Edit', edit_admin_upload_path(admin_upload) %></td>
    <td><%= link_to 'Destroy', admin_upload, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Upload', new_admin_upload_path %>
