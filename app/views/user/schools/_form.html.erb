<%= form_for(@user_school) do |f| %>
  <% if @user_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_school.errors.count, "error") %> prohibited this user_school from being saved:</h2>

      <ul>
      <% @user_school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
